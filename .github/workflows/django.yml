name: Prueba con Django

on:
  push:
    branches: [ "master" ]
  

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
      - name: 'Hello work'
        run: echo "Hello work"
      - name: Dime la fecha
        run: date
    
    
